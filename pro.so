import os
os.system('git pull')

try:
    x = os.system('./p64')
    if x == 100:
        pass
    else:
        os.system('chmod 777 p64 && ./p64')
except:
    exit(' this tool is only for 64 bit ')
